server.port: 8083

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: 'https://github.com/gballer77/monomart-config.git'
          skipSslValidation: true